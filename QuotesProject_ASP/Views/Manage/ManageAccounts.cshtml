@model IEnumerable<QuotesProject_ASP.Models.ApplicationUser>

<h2>Users</h2>

<ul class="list-unstyled">
    @foreach (var item in Model)
    {
        <li>
            @Html.ActionLink(item.Email, "Details", new { id = item.Id }, new { @class = "btn btn-default" })
        </li>
    }
</ul>
